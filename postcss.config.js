// postcss.config.js
module.exports = {
    plugins: {
      // use the new, separate PostCSS plugin package:
      '@tailwindcss/postcss': {},
      // autoprefixer must come after:
      autoprefixer: {},
    },
  }
  